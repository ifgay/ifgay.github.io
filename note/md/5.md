# python 

```python
tm=(12,34,)#元组 不可变
arr=[23,345]#数组 可改变
del tmp[1] #删除
setE={'a','b'}#不重复 set类型 集合

#迭代器
>>> list=[1,2,3,4]
>>> it = iter(list)    # 创建迭代器对象
>>> print (next(it))   # 输出迭代器的下一个元素
1
>>> print (next(it))
2
>>>
ok , 现在你已经入门python了



```

